name: 📦构建027魔改版本
run-name: 📦 ${{ github.actor }} @ 构建027魔改版本

on:
  push:
    branches: ["my"]
  pull_request:
    branches: ["my"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set Timezone to Asia/Shanghai
        run: |
          sudo timedatectl set-timezone Asia/Shanghai
          echo "Current time: $(date)"

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}  # 支持 PR/Push

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "zulu"
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false
          gradle-version: wrapper

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew assembleNormalRelease assembleCompatibleRelease -Pversion=${{ github.ref_name }}

      - name: Locate APKs and Set Outputs
        id: locate_apks
        run: |
          normal_apk=$(find app/build/outputs/apk/normal/release -name "*.apk" | head -n 1)
          compatible_apk=$(find app/build/outputs/apk/compatible/release -name "*.apk" | head -n 1)
          echo "normal_apk=$normal_apk" >> $GITHUB_OUTPUT
          echo "compatible_apk=$compatible_apk" >> $GITHUB_OUTPUT

      # 签名相关步骤已注释（正确）
      # - name: Copy APKs to staging for signing
      # - name: Sign APKs  
      # - name: Extract Signed APK Paths

      - name: Upload APK - Compatible
        uses: actions/upload-artifact@v4
        with:
          name: apk-compatible
          path: ${{ steps.locate_apks.outputs.compatible_apk }}

      - name: Upload APK - Normal
        uses: actions/upload-artifact@v4
        with:
          name: apk-normal
          path: ${{ steps.locate_apks.outputs.normal_apk }}

      - name: Extract file names
        id: extract_info
        run: |
          # 修改：使用原始APK文件路径
          normal_file=$(basename "${{ steps.locate_apks.outputs.normal_apk }}")
          compatible_file=$(basename "${{ steps.locate_apks.outputs.compatible_apk }}")
          # 修改：调整正则表达式，移除对-signed的匹配
          version=$(echo "$normal_file" | sed -E 's/.*-(Normal|Compatible)-(v[^-]+).*\.apk/\2/')
          echo "normal_file=$normal_file" >> $GITHUB_OUTPUT
          echo "compatible_file=$compatible_file" >> $GITHUB_OUTPUT
          echo "version=$version" >> $GITHUB_OUTPUT

      - name: Generate APKS Checksum
        run: |
          # 修改：使用原始APK文件路径，文件名也要相应修改
          sha256sum ${{ steps.locate_apks.outputs.normal_apk }} > CHECKSUMS-Sesame-Normal-${{ steps.extract_info.outputs.version }}.${{ env.SHORT_SHA }}.apk.sha256
          sha256sum ${{ steps.locate_apks.outputs.compatible_apk }} > CHECKSUMS-Sesame-Compatible-${{ steps.extract_info.outputs.version }}.${{ env.SHORT_SHA }}.apk.sha256

      - name: Create Tag from Version
        # 修改：条件改为匹配 'my' 分支
        if: startsWith(github.ref, 'refs/heads/my') && github.event_name == 'push'
        run: |
          VERSION_TAG="v${{ steps.extract_info.outputs.version }}"
          echo "Creating tag: $VERSION_TAG"
          git tag "$VERSION_TAG"
          git push origin "$VERSION_TAG"
      - name: Get current time
        id: get_time
        if: startsWith(github.ref, 'refs/heads/my') && github.event_name == 'push'
        run: |
          echo "time=$(date '+%Y-%m-%d %H:%M:%S %Z')" >> $GITHUB_OUTPUT

      - name: Create Release with Assets
        if: startsWith(github.ref, 'refs/heads/my') && github.event_name == 'push'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.extract_info.outputs.version }}
          name: 📦 Sesame-TK v${{ steps.extract_info.outputs.version }}
          body: |
            ## 🚀 Sesame-TK v${{ steps.extract_info.outputs.version }} 发布
            
            ### 📱 下载说明
            - **Normal版本**: 适用于大部分Android设备
            - **Compatible版本**: 兼容性更好，适用于较老设备或特殊ROM
            
            ### 📝 本次更新
            ${{ github.event.headcommit.message || '手动构建发布' }}
            
            ### 👤 提交者
            ${{ github.event.head_commit.author.name || github.actor }}
            
            ### 🔗 详细信息
            - 提交SHA: `${{ github.sha }}`
            - 构建时间: ${{ steps.get_time.outputs.time }}
            - [查看提交详情](${{ github.event.head_commit.url }})
          files: |
            ${{ steps.locate_apks.outputs.normal_apk }}
            ${{ steps.locate_apks.outputs.compatible_apk }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            
